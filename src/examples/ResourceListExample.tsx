import React from 'react';
import { ResourceList } from '..';

export default () => (
  <div>
    <h1>Resource List</h1>
    <section className="mt-4">
      <ResourceList />
    </section>
  </div>
);
